.center_content
  #right_wrap
    #right_content
      %h2 Login

      .form
        = form_for :session, url: login_path do |f|
          .form_row
            = f.label :password
            = f.password_field :password, class: "form_input"
          .fom_row
            = f.submit "Login", class: "form_submit"

  .sidebar{id: "sidebar"}
    %h2 Autenticar
    .sidebar_section_text Por favor ingresa tus credenciales validos.
